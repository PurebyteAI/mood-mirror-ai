# Web framework
fastapi>=0.110.0
uvicorn>=0.25.0
starlette>=0.37.0
python-multipart>=0.0.9

# Data / validation
pydantic>=2.0.0

# Database
aiosqlite>=0.19.0

# Config
python-dotenv>=1.0.0

# HTTP client (used by openrouter_service.py)
httpx>=0.27.0

# OpenRouter / OpenAI-compatible client (for AI analysis + Groq STT)
openai>=1.0.0
groq

# LiveKit voice AI (Talk tab)
livekit~=1.0
livekit-agents[openai,silero,rime]~=1.4

# Payments (reserved for future use)
stripe>=5.0.0
